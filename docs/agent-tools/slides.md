# Slides agent tools

<!-- Generated by tools/agent-tool-docs.mjs from the tool definitions. Do not edit by hand. -->

49 tools, as the model sees them: the name it calls, the description it is given, and every argument its schema accepts.

## `add_chart`

Insert a chart on a page (native pptx chart, still editable in PowerPoint). categories are the x-axis categories; series is each series' name and values (length must match categories). Omit x/y/w/h to center it. dataSource declares where the numbers came from and is enforced — never present invented numbers as real data. Pick by the question the chart answers: bar for comparing categories, barH when the labels are long, line for change over time, area for a cumulative total, pie or doughnut only for parts of one whole with few slices, barStacked for composition, barPercentStacked when the shares matter more than the totals, scatter for the relationship between two measures, radar to compare several dimensions of a few items, comboBarLine when one series is a different unit from the others (volume plus rate) — the last series becomes the line.

| argument          | type                                                                                                                                        | required | description                                                                                                                                                                                                                                       |
| ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `slideIndex`      | integer                                                                                                                                     | yes      |                                                                                                                                                                                                                                                   |
| `kind`            | `bar` \| `barStacked` \| `barPercentStacked` \| `barH` \| `line` \| `area` \| `pie` \| `doughnut` \| `scatter` \| `radar` \| `comboBarLine` | yes      |                                                                                                                                                                                                                                                   |
| `title`           | string                                                                                                                                      |          | Chart title (optional)                                                                                                                                                                                                                            |
| `categories`      | string[]                                                                                                                                    | yes      |                                                                                                                                                                                                                                                   |
| `series`          | object[]                                                                                                                                    | yes      |                                                                                                                                                                                                                                                   |
| `series[].name`   | string                                                                                                                                      | yes      |                                                                                                                                                                                                                                                   |
| `series[].values` | number[]                                                                                                                                    | yes      |                                                                                                                                                                                                                                                   |
| `dataSource`      | `user` \| `document` \| `search` \| `sample`                                                                                                | yes      | Provenance of the values: 'user' = supplied by the user/attachments, 'document' = read from this deck, 'search' = from web_search results in this conversation (run it first), 'sample' = illustrative placeholders you must disclose to the user |
| `x`               | number                                                                                                                                      |          |                                                                                                                                                                                                                                                   |
| `y`               | number                                                                                                                                      |          |                                                                                                                                                                                                                                                   |
| `w`               | number                                                                                                                                      |          |                                                                                                                                                                                                                                                   |
| `h`               | number                                                                                                                                      |          |                                                                                                                                                                                                                                                   |

## `add_equation`

Place a mathematical formula on a page as a real PowerPoint equation, written in LaTeX. Use it for anything with a fraction, root, exponent, sum, integral or Greek letter — a text box with "x^2" in it is not an equation and does not typeset. Supports the common LaTeX subset (\frac \sqrt \sum \int \lim, super/subscripts, matrices, Greek); the align environment is not supported and invalid LaTeX is rejected with the fragment it stopped on. Our canvas draws the LaTeX source rather than typeset maths — PowerPoint typesets it.

| argument     | type                          | required | description                                        |
| ------------ | ----------------------------- | -------- | -------------------------------------------------- |
| `slideIndex` | integer                       | yes      | Page number (0-based)                              |
| `latex`      | string                        | yes      | LaTeX source, e.g. "E = mc^2" or "\frac{a}{b}"     |
| `x`          | number                        |          | Position and size in canvas pixels; omit to center |
| `y`          | number                        |          |                                                    |
| `w`          | number                        |          |                                                    |
| `h`          | number                        |          |                                                    |
| `align`      | `left` \| `center` \| `right` |          |                                                    |
| `fontSizePt` | number                        |          | Point size; defaults to the theme size             |

## `add_shape`

Create a new shape on a page (optionally with solid fill and text). kind uses OOXML preset geometry names, common ones: rect/roundRect/ellipse/triangle/diamond/rightArrow/leftArrow/chevron/star5/heart/pie/donut/cloud/wedgeRoundRectCallout. Returns the new element id.

| argument     | type    | required | description                                                                      |
| ------------ | ------- | -------- | -------------------------------------------------------------------------------- |
| `slideIndex` | integer | yes      |                                                                                  |
| `kind`       | string  | yes      | OOXML preset geometry name, e.g. rect / roundRect / ellipse / rightArrow / star5 |
| `x`          | number  | yes      |                                                                                  |
| `y`          | number  | yes      |                                                                                  |
| `w`          | number  | yes      |                                                                                  |
| `h`          | number  | yes      |                                                                                  |
| `fillColor`  | string  |          | #RRGGBB                                                                          |
| `paragraphs` | any     |          |                                                                                  |

## `add_slide`

Create a new page by cloning the layout (including background) of page sourceIndex, inserted right after it (new page number = sourceIndex+1; pages after it shift back); clearText=true (default) clears text to get a layout-preserving blank page. When building page by page, use the CURRENT LAST page as sourceIndex so new pages append at the end. The return value gives the new page's slideIndex; subsequent content fills MUST use that returned page number.

| argument      | type    | required | description                                        |
| ------------- | ------- | -------- | -------------------------------------------------- |
| `sourceIndex` | integer | yes      | Page to use as the layout template (0-based)       |
| `clearText`   | boolean |          | Default true; false keeps the template page's text |

## `add_smartart`

Insert a SmartArt-style diagram (shape composition) on a page: list=vertical list, process=process arrows, cycle=cycle, hierarchy=org structure, pyramid=stacked pyramid levels, matrix=2x2 quadrant grid, venn=overlapping circles. items are the node texts. Omit x/y/w/h to center it.

| argument     | type                                                                             | required | description                  |
| ------------ | -------------------------------------------------------------------------------- | -------- | ---------------------------- |
| `slideIndex` | integer                                                                          | yes      |                              |
| `layout`     | `list` \| `process` \| `cycle` \| `hierarchy` \| `pyramid` \| `matrix` \| `venn` | yes      |                              |
| `items`      | string[]                                                                         | yes      | Node texts (2-8 recommended) |
| `x`          | number                                                                           |          |                              |
| `y`          | number                                                                           |          |                              |
| `w`          | number                                                                           |          |                              |
| `h`          | number                                                                           |          |                              |

## `add_table`

Insert a native pptx table on a page (with built-in styling, still editable in PowerPoint). cells gives text row by row (optional; missing rows/columns stay empty). Omit x/y/w/h to center it.

| argument     | type       | required | description                                             |
| ------------ | ---------- | -------- | ------------------------------------------------------- |
| `slideIndex` | integer    | yes      |                                                         |
| `rows`       | integer    | yes      | Row count (including header)                            |
| `cols`       | integer    | yes      | Column count                                            |
| `cells`      | string[][] |          | Cell texts, row by row, e.g. [["Name","Qty"],["A","1"]] |
| `x`          | number     |          |                                                         |
| `y`          | number     |          |                                                         |
| `w`          | number     |          |                                                         |
| `h`          | number     |          |                                                         |

## `add_text_box`

Create a new text box on a page (pixel coordinates). Returns the new element id.

| argument     | type    | required | description |
| ------------ | ------- | -------- | ----------- |
| `slideIndex` | integer | yes      |             |
| `x`          | number  | yes      |             |
| `y`          | number  | yes      |             |
| `w`          | number  | yes      |             |
| `h`          | number  | yes      |             |
| `paragraphs` | any     | yes      |             |

## `analyze_media`

Analyze media content (Genspark): understand images/audio/video. Pass media URLs (or local file paths) and analysis requirements; returns analysis text. Video supports extracting key points, structure, and time ranges — good for turning user material into usable deck content.

| argument       | type     | required | description                                                                                                           |
| -------------- | -------- | -------- | --------------------------------------------------------------------------------------------------------------------- |
| `mediaUrls`    | string[] | yes      | List of media URLs or local file paths                                                                                |
| `requirements` | string   | yes      | Analysis requirements (English): what to extract and how the result will be used (e.g. extract key points for slides) |

## `ask_clarification`

[Call before creating a whole new deck] Shows a questionnaire card with options, letting the user make key choices for this deck (audience/scenario/tone/focus etc.); the user's choices directly determine the deck's Core Hook and style. Questions must target the specific topic, each being a real trade-off (options represent different directions). Ask 2–4 questions, ≤5 options each. After calling, wait for the user to finish choosing in the card and generate once you have the answers. Don't repeat the questions in your reply text.

| argument                  | type     | required | description                                                                  |
| ------------------------- | -------- | -------- | ---------------------------------------------------------------------------- |
| `questions`               | object[] | yes      | Question list (2–4 questions)                                                |
| `questions[].id`          | string   | yes      | Unique question id (short English/pinyin)                                    |
| `questions[].label`       | string   | yes      | Question text                                                                |
| `questions[].description` | string   |          | Optional one-line note                                                       |
| `questions[].options`     | string[] | yes      | Options (≤5); the frontend automatically appends "Decide for me" and "Other" |
| `questions[].multi`       | boolean  |          | Multi-select (single-select by default)                                      |

## `delete_element`

Delete one element from a page.

| argument     | type    | required | description |
| ------------ | ------- | -------- | ----------- |
| `slideIndex` | integer | yes      |             |
| `sourceId`   | string  | yes      |             |

## `delete_slide`

Delete an entire page (not allowed when only one page remains). After deletion, later pages' slideIndex shifts down.

| argument     | type    | required | description           |
| ------------ | ------- | -------- | --------------------- |
| `slideIndex` | integer | yes      | Page number (0-based) |

## `edit_chart`

Modify a chart (including charts from imported files; first edit converts it to editable automatically): change type/data/colors/chart elements. kind options: bar/barStacked/line/area/pie/doughnut. colorScheme: default/colorful/colorful2/mono-accent1..6 (theme-derived); legacy keys blue/warm/cool/mono still work.

| argument          | type                                                                                                                                        | required | description                                                                                                                                                             |
| ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `slideIndex`      | integer                                                                                                                                     | yes      |                                                                                                                                                                         |
| `sourceId`        | string                                                                                                                                      | yes      | Chart element id (type=chart)                                                                                                                                           |
| `kind`            | `bar` \| `barStacked` \| `barPercentStacked` \| `barH` \| `line` \| `area` \| `pie` \| `doughnut` \| `scatter` \| `radar` \| `comboBarLine` |          | Change chart type (optional)                                                                                                                                            |
| `categories`      | string[]                                                                                                                                    |          | X-axis/category labels (optional)                                                                                                                                       |
| `series`          | object[]                                                                                                                                    |          | Data series (optional)                                                                                                                                                  |
| `series[].name`   | string                                                                                                                                      | yes      |                                                                                                                                                                         |
| `series[].values` | number[]                                                                                                                                    | yes      |                                                                                                                                                                         |
| `dataSource`      | `user` \| `document` \| `search` \| `sample`                                                                                                |          | Required when passing series: provenance of the values ('user'/'document'/'search'/'sample'; 'search' needs a prior web_search, 'sample' must be disclosed to the user) |
| `colorScheme`     | string                                                                                                                                      |          | Color scheme (optional): default/colorful/colorful2/mono-accent1..6                                                                                                     |
| `title`           | string                                                                                                                                      |          | Chart title (optional)                                                                                                                                                  |
| `legendPos`       | `b` \| `t` \| `r` \| `l` \| `none`                                                                                                          |          | Legend position (optional)                                                                                                                                              |
| `dataLabels`      | boolean                                                                                                                                     |          | Data labels toggle (optional)                                                                                                                                           |
| `gridlines`       | boolean                                                                                                                                     |          | Value-axis gridlines toggle (optional)                                                                                                                                  |
| `switchRowCol`    | boolean                                                                                                                                     |          | Switch rows/columns: categories ↔ series (optional)                                                                                                                     |

## `edit_raw_xml`

Replace one exact occurrence of find with replace inside an XML part. Use only when no other tool can express the change — normal edits go through the element tools, which understand the document model. find must occur exactly once, so copy enough surrounding text from read_raw_xml to be unambiguous; the edit is rejected if it matches zero or several times, if the result is not well-formed XML, or if it leaves a slide unparseable, and the part is left untouched. Well-formed XML can still be invalid OOXML that PowerPoint refuses to open, so keep edits small and tell the user what you changed.

| argument  | type   | required | description                                                    |
| --------- | ------ | -------- | -------------------------------------------------------------- |
| `part`    | string | yes      | Part reference, e.g. '/slide[1]' or '/theme'                   |
| `find`    | string | yes      | Exact XML text to replace; must occur exactly once in the part |
| `replace` | string | yes      | Replacement XML; empty string deletes the match                |

## `edit_table_cell`

Replace one table cell's text entirely. The table element id comes from the outline/read_slide (type=table); row/col are 0-based.

| argument     | type    | required | description             |
| ------------ | ------- | -------- | ----------------------- |
| `slideIndex` | integer | yes      |                         |
| `sourceId`   | string  | yes      | Table element id        |
| `row`        | integer | yes      | Row number (0-based)    |
| `col`        | integer | yes      | Column number (0-based) |
| `paragraphs` | any     | yes      |                         |

## `edit_table_structure`

Add/remove table rows/columns: kind=insert-row/delete-row/insert-col/delete-col; index is the row/column number (0-based), insert defaults to after it, before=true inserts before it.

| argument     | type                                                         | required | description                                 |
| ------------ | ------------------------------------------------------------ | -------- | ------------------------------------------- |
| `slideIndex` | integer                                                      | yes      |                                             |
| `sourceId`   | string                                                       | yes      | Table element id                            |
| `kind`       | `insert-row` \| `delete-row` \| `insert-col` \| `delete-col` | yes      |                                             |
| `index`      | integer                                                      | yes      | Row/column number (0-based)                 |
| `before`     | boolean                                                      |          | For insert, set true to insert before index |

## `edit_table_style`

Modify table styling: apply a preset (styleName) or individually change header row/banding/shading/borders. styleName options: none/lightGrid/zebraBlue/zebraGray/headerDarkBlue/headerOrange/noBorder/fullBorder.

| argument        | type            | required | description                                           |
| --------------- | --------------- | -------- | ----------------------------------------------------- |
| `slideIndex`    | integer         | yes      |                                                       |
| `sourceId`      | string          | yes      | Table element id                                      |
| `styleName`     | string          |          | Preset style name (see description), highest priority |
| `firstRow`      | boolean         |          | Enable header row (first-row emphasis)                |
| `bandRow`       | boolean         |          | Enable banded rows                                    |
| `shadingColor`  | string          |          | Shading color #RRGGBB, "none" clears shading          |
| `borderColor`   | string          |          | Border color #RRGGBB                                  |
| `borderWidthPt` | number          |          | Border width (pt)                                     |
| `borderPreset`  | `all` \| `none` |          | "all" = full borders, "none" = clear borders          |

## `execute_slide_script`

[Preferred tool for editing a slide's existing elements] Runs your JS edit script against one page; a single script covers: position/size/alignment/distribution/relative nudges/text/style/fill/stroke. At run time the script automatically receives the real geometry and text of every element on the page (els) — **no read_slide needed first**; read-write combined, compute from els inside the script. Geometry changes are applied atomically in one batch (undoable as a whole), the rest in script order, and a layout audit (overlap/out-of-bounds/text overflow) is returned at the end. Far more reliable than individual set_element_* calls — coordinate math happens at execution site, not from memory. If the audit reports problems, call this tool again immediately to fix. Script environment (constrained synchronous JS-like DSL; no external APIs or ambient globals): - els: array, each item {id,type,text,x,y,w,h,rotation,fontSizePt?,fill?,textColor?,strokeColor?,inGroup?,groupId?,locked?} (pixels, origin top-left; fill/textColor/strokeColor are current colors in #RRGGBB, read-only — write via setFill/setStyle/setStroke; inGroup+groupId=directly editable group child (all primitives work, coordinates absolute as shown); inGroup without groupId=nested in a sub-group, read-only — ungroup_element the outer group first; locked=layout decoration, read-only) - canvas: {w,h} canvas size (px) - setBox(id, {x?,y?,w?,h?,rotation?}): set an element's target box, pass only fields to change - moveBy(id, dx, dy): relative move (left = negative dx, up = negative dy) - resizeBy(id, dw, dh): relative resize - setText(id, textOrParagraphs): replace text entirely; pass a string (split into paragraphs by \n) or a paragraph array (same format as set_element_text's paragraphs) - setStyle(id, {fontSize?,color?,bold?,italic?,underline?,align?,fontFamily?}): change style without changing text, pass only fields to change - setFill(id, colorOrNone): solid fill '#RRGGBB' or 'none' - setStroke(id, {color?,widthPt?} | null): stroke; pass null to remove - log(...): debug output (echoed back to you); the return value is echoed back to you (put a summary there) - Supported computation: const/let, arithmetic, if/for/for...of/while, functions/arrows, JSON object/array literals, Math, regex.test, and safe array/string methods. No classes, async, modules, constructors, prototypes, or dynamic code. Example 1 — three cards equal width, equal spacing: const cards = els.filter(e => /card/.test(e.id)); const gap = 32, w = (canvas.w - 2*80 - (cards.length-1)_gap) / cards.length; cards.forEach((c, i) => setBox(c.id, { x: 80 + i_(w+gap), y: 200, w, h: 320 })); Example 2 — move the title left a bit: moveBy('title', -30, 0); Example 3 — make the title blue and bold: setStyle('t1', { color: '#1a73e8', bold: true });

| argument      | type    | required | description                                                                                                                                    |
| ------------- | ------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| `slideIndex`  | integer | yes      | Page number (0-based)                                                                                                                          |
| `code`        | string  | yes      | JS script body (synchronous code; may use els/canvas plus setBox/moveBy/resizeBy/setText/setStyle/setFill/setStroke/log; may return a summary) |
| `explanation` | string  |          | One sentence describing what this script does (≤60 chars, shown to the user)                                                                   |

## `generate_deck`

[First choice for creating a whole new deck — self-driven pipeline: auto image search, page-by-page generation with live display, no missing pages] Recommended usage (especially with many pages): pass only topic + approx_pages (+ optional style/context); the system plans the outline internally (auto-batched beyond 12 pages), **auto-searches images** (no advance image_search — the system searches from the planned image_queries keywords internally and fills real URLs back before writing HTML), writes HTML page by page, and lands pages onto the canvas one by one. You don't hand-write dozens of pages, and neither "only page 1 got generated" nor "arguments were truncated" can happen — the page count is guaranteed by the system loop. (If you already know each page you may pass core_hook+style+pages directly; pages[].image_queries takes English image-search keywords, searched internally; if you already know real http(s) URLs pass them directly — the system respects existing URLs and does not re-search.) To add a few pages to an existing deck, pass pages (briefs for just the new pages) + insert_mode:"append".

| argument                | type                                         | required | description                                                                                                                                                                                                                  |
| ----------------------- | -------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `topic`                 | string                                       |          | [Recommended] The deck's topic/requirements description (with topic you don't hand-write pages; the system plans internally)                                                                                                 |
| `approx_pages`          | integer                                      |          | Expected page count (used together with topic)                                                                                                                                                                               |
| `context`               | string                                       |          | Optional: real material/data/questionnaire answers from web_search, so internal planning uses real content                                                                                                                   |
| `core_hook`             | string                                       |          | Optional: a narrative anchor you already decided (recommended alongside pages)                                                                                                                                               |
| `style`                 | string                                       |          | Unified design system: primary/secondary colors, fonts, content margins, card corners (required with pages; optional as a style hint with topic)                                                                             |
| `pages`                 | object[]                                     |          | Optional: pass directly when you already know each page (as many pages generated as planned). With many pages prefer topic and internal planning, to avoid over-long truncated arguments                                     |
| `pages[].title`         | string                                       | yes      | Page title                                                                                                                                                                                                                   |
| `pages[].type`          | string                                       |          | cover\|content\|data\|closing                                                                                                                                                                                                |
| `pages[].brief`         | string                                       | yes      | Page content description (use real data/facts)                                                                                                                                                                               |
| `pages[].layout`        | string                                       | yes      | Layout (content pages must not repeat)                                                                                                                                                                                       |
| `pages[].image_queries` | string[]                                     |          | English image-search keywords for this page's image slots (the system searches internally and fills real URLs back); if you already know real http(s) URLs pass them directly (respected, not re-searched); [] for no images |
| `insert_mode`           | `replace` \| `append`                        |          | replace (default, new whole deck) = replace everything; append = append at the end                                                                                                                                           |
| `style_template`        | string                                       |          | Optional: name of a saved style template (from list_style_templates); when passed, Step 0 is skipped and the template's styleSkill is used directly, no style regeneration                                                   |
| `dataSource`            | `user` \| `document` \| `search` \| `sample` |          | Required when topic/context/briefs carry specific figures (%, money, magnitudes): where they came from — 'user'/'document'/'search' (run web_search first)/'sample' (disclose to the user)                                   |

## `generate_image`

AI image generation/editing (Genspark). Text-to-image, or pass referenceImageUrls for image editing; returns an image URL, then insert with insert_web_image. Use for custom illustrations/icons/backgrounds, style-consistent imagery, and edits like background removal/upscaling/outpainting; for real photos/screenshots still use image_search.

| argument             | type     | required | description                                                                                                                                                                                                                               |
| -------------------- | -------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `prompt`             | string   | yes      | Image description, English works better (keep any text to render in the image verbatim)                                                                                                                                                   |
| `model`              | string   |          | Optional, defaults to the general model. Specify only for special purposes: fal-bria-rmbg=background removal, fal-ai/recraft-clarity-upscale=upscale, flux-pro/outpaint=outpaint, fal-ai/image-editing/text-removal=remove text watermark |
| `referenceImageUrls` | string[] |          | URLs of reference images / images to edit (required for editing tasks)                                                                                                                                                                    |
| `aspectRatio`        | string   |          | Aspect ratio: 1:1\|4:3\|16:9\|9:16\|3:4\|2:3\|3:2\|auto                                                                                                                                                                                   |

## `get_deck_context`

Get the deck's latest outline: per-page list of text elements (element id | type | text preview). Call to confirm global state after edits.

No arguments.

## `group_elements`

Group two or more elements so they move, resize and are styled as one — the counterpart of ungroup_element. Use after building something out of parts (a labelled icon, a callout with its arrow) so a later layout change keeps it together. Grouping rewrites the page, so element ids on it change; the result returns the new group id.

| argument     | type     | required | description                              |
| ------------ | -------- | -------- | ---------------------------------------- |
| `slideIndex` | integer  | yes      | Page number (0-based)                    |
| `sourceIds`  | string[] | yes      | Ids of at least two elements on the page |

## `image_search`

Search image assets (for slide imagery). Returns a list of imageUrl; after choosing, insert with insert_web_image.

| argument     | type    | required | description                                  |
| ------------ | ------- | -------- | -------------------------------------------- |
| `query`      | string  | yes      | Image search keywords (English works better) |
| `maxResults` | integer |          | Max results, default 8                       |

## `insert_diagram`

Draw a flowchart from Mermaid source as native shapes and arrows the user can then move and restyle. Use it for a process, a decision tree or a dependency graph — anything add_smartart's seven fixed layouts cannot express. Only flowcharts: start the source with "flowchart TD" (top-down) or "flowchart LR" (left-right). Node shapes follow Mermaid: A[box], A(rounded), A{decision}, A((circle)); label an arrow with -->|text|.

| argument     | type    | required | description                                                               |
| ------------ | ------- | -------- | ------------------------------------------------------------------------- |
| `slideIndex` | integer | yes      |                                                                           |
| `mermaid`    | string  | yes      | Mermaid flowchart source, e.g. "flowchart TD; A[Submit] --> B{Approved?}" |
| `x`          | number  |          | area for the diagram; omit to use the page body                           |
| `y`          | number  |          |                                                                           |
| `w`          | number  |          |                                                                           |
| `h`          | number  |          |                                                                           |

## `insert_web_image`

Download an image URL obtained from image_search or generate_image and insert it into a page (pixel coordinates).

| argument     | type    | required | description                                    |
| ------------ | ------- | -------- | ---------------------------------------------- |
| `slideIndex` | integer | yes      |                                                |
| `url`        | string  | yes      | Direct image link (imageUrl from image_search) |
| `x`          | number  | yes      |                                                |
| `y`          | number  | yes      |                                                |
| `w`          | number  | yes      |                                                |
| `h`          | number  | yes      |                                                |

## `list_style_templates`

List all saved style templates (name + topic + createdAt). When the user says "use last time's style" or "use some template", call this first to see what exists, then pass the target template name to generate_deck's style_template argument.

No arguments.

## `manage_comments`

Read or leave review comments on a page. Use it to answer questions about the review state, or to leave a note for a colleague — not to talk to the user, who is reading this conversation. action=list returns the comments on a page; add leaves one under the app user's name; remove takes authorId and idx from list.

| argument     | type                        | required | description              |
| ------------ | --------------------------- | -------- | ------------------------ |
| `action`     | `list` \| `add` \| `remove` | yes      |                          |
| `slideIndex` | integer                     | yes      |                          |
| `text`       | string                      |          | add: the comment body    |
| `authorId`   | integer                     |          | remove: from action=list |
| `idx`        | integer                     |          | remove: from action=list |

## `manage_sections`

Group pages into named sections, the way a long deck is divided for navigation and for presenting one part of it. action=list returns the current sections with the pages in each; add starts a section at a page (it runs to the next section or the end); rename and remove take an id from list — removing a section keeps its pages.

| argument       | type                                              | required | description                                 |
| -------------- | ------------------------------------------------- | -------- | ------------------------------------------- |
| `action`       | `list` \| `add` \| `rename` \| `remove` \| `move` | yes      |                                             |
| `atSlideIndex` | integer                                           |          | add: the page the section starts at         |
| `name`         | string                                            |          | add / rename: section name                  |
| `id`           | string                                            |          | rename / remove / move: id from action=list |
| `dir`          | `up` \| `down`                                    |          | move: direction                             |

## `move_slide`

Move a page to a different position in the deck. Indexes are 0-based and describe the order before the move; sections follow their pages. Use it to fix a running order rather than deleting and rebuilding a page.

| argument    | type    | required | description |
| ----------- | ------- | -------- | ----------- |
| `fromIndex` | integer | yes      |             |
| `toIndex`   | integer | yes      |             |

## `plan_deck`

[When creating a whole new deck, call after researching material/images and before generate_deck] Outputs a structured plan: the Core Hook + unified style scheme + each page's title/content brief/layout/image keywords. Think the whole deck through first, to avoid starting strong and fizzling out. The plan is echoed to the user.

| argument                | type     | required | description                                                                                                                                                                          |
| ----------------------- | -------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `core_hook`             | string   | yes      | The deck's narrative anchor (one sentence, with tension, ideally containing a number or counter-intuitive contrast)                                                                  |
| `style`                 | string   | yes      | Unified design system: primary/secondary colors, font tone, content margins, card/corner style (e.g. "dark blue primary + gold accents, data-dashboard look"); every page follows it |
| `pages`                 | object[] | yes      | Per-page plan                                                                                                                                                                        |
| `pages[].title`         | string   | yes      | Page title                                                                                                                                                                           |
| `pages[].type`          | string   |          | cover\|content\|data\|closing                                                                                                                                                        |
| `pages[].brief`         | string   | yes      | Page content description (use real data/facts; say what goes in each region)                                                                                                         |
| `pages[].layout`        | string   | yes      | Layout (e.g. three_column_cards/hero_big_number/two_column/timeline/left_text_right_image); content pages must not repeat                                                            |
| `pages[].image_queries` | string[] |          | English image-search keywords for this page's image slots (one per slot; [] for no images)                                                                                           |

## `read_attachment`

Read an attachment's text content (parsed locally). Long files are paginated: read offset=0 first, then decide whether to continue based on the returned total character count.

| argument | type    | required | description                                         |
| -------- | ------- | -------- | --------------------------------------------------- |
| `index`  | integer | yes      | Attachment index (0-based, see the attachment list) |
| `offset` | integer |          | Starting character position, default 0              |

## `read_raw_xml`

Read one XML part of the .pptx package. The last resort for something no other tool exposes — an effect, an inherited property, a content-type override. Part references: '/slide[1]' (1-based, follows the visible order), '/presentation', '/theme', '/slideLayout[2]', '/slideMaster[1]', '/notesSlide[1]', or a literal zip path like 'ppt/slides/slide1.xml'. Omit part to list what is in the package. Always read before writing: edit_raw_xml matches text exactly.

| argument | type   | required | description                                                   |
| -------- | ------ | -------- | ------------------------------------------------------------- |
| `part`   | string |          | Part reference; omit to list the parts instead of reading one |

## `read_slide`

Read all elements of a page with full text (untruncated) and current colors (fill/text/stroke, hex). Call before rewriting a page.

| argument     | type    | required | description           |
| ------------ | ------- | -------- | --------------------- |
| `slideIndex` | integer | yes      | Page number (0-based) |

## `regenerate_slide`

[Redo/redesign an existing page] The cloud service regenerates the page from your brief and replaces it in place (other pages untouched, undoable). Use when the user says "redo this page / redesign it / try another layout / make it prettier"; don't dismantle the page element by element with native tools. Flow: first read_slide to get the page's current content, then check neighboring pages / get_deck_context to grasp the deck's style; write a detailed brief — what to keep (copy real text/data into the brief verbatim), what to change, and the target layout; the deck style is applied automatically. If the page needs images, image_search first and pass real URLs in image_urls. If cloud generation fails, it is usually a temporary service error: do NOT loop retrying — make the concrete changes in place with execute_slide_script instead (or tell the user to try again in a few minutes).

| argument     | type                                         | required | description                                                                                                                                                                                                |
| ------------ | -------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `slideIndex` | integer                                      | yes      | Page to redo (0-based)                                                                                                                                                                                     |
| `brief`      | string                                       | yes      | Content and layout brief for the new page: what goes in each region (copy the real copy/data to keep into the brief), and the layout to use (e.g. three_column_cards/hero_big_number/two_column/timeline). |
| `title`      | string                                       |          | Page title                                                                                                                                                                                                 |
| `layout`     | string                                       |          | Layout intent name (optional)                                                                                                                                                                              |
| `image_urls` | string[]                                     |          | Real http(s) image URLs for this page (image_search first; [] for none)                                                                                                                                    |
| `dataSource` | `user` \| `document` \| `search` \| `sample` |          | Required when the brief carries specific figures (%, money, magnitudes): where they came from — 'user'/'document'/'search' (run web_search first)/'sample' (disclose to the user)                          |

## `reorder_element`

Move an element through the stacking order — which one wins where they overlap. front/back jump to the top or bottom of the page; forward/backward move one step. Use it when a shape hides text, or to put a backdrop rectangle behind everything.

| argument     | type                                         | required | description           |
| ------------ | -------------------------------------------- | -------- | --------------------- |
| `slideIndex` | integer                                      | yes      | Page number (0-based) |
| `sourceId`   | string                                       | yes      |                       |
| `dir`        | `front` \| `back` \| `forward` \| `backward` | yes      |                       |

## `save_style_template`

[Save the current deck's style as a reusable template] Saves the current presentation's Style Skill (visual style guide) under the given name; next time you generate a deck, pass the style_template argument to reuse it directly and skip style generation. Call when the user says "save this style" / "save as template".

| argument | type   | required | description                                               |
| -------- | ------ | -------- | --------------------------------------------------------- |
| `name`   | string | yes      | Template name (short, e.g. "minimal-blue" or "tech-dark") |

## `set_element_fill`

Set an element's solid fill. fill=#RRGGBB; pass "none" for no fill.

| argument     | type    | required | description     |
| ------------ | ------- | -------- | --------------- |
| `slideIndex` | integer | yes      |                 |
| `sourceId`   | string  | yes      |                 |
| `fill`       | string  | yes      | #RRGGBB or none |

## `set_element_link`

Make an element a hyperlink — to a URL, or to another page in this deck (for an agenda or a "back to contents" control). Pass neither url nor targetSlideIndex to remove the link.

| argument           | type    | required | description                                   |
| ------------------ | ------- | -------- | --------------------------------------------- |
| `slideIndex`       | integer | yes      |                                               |
| `sourceId`         | string  | yes      | Element id from the deck outline / read_slide |
| `url`              | string  |          | http(s) target                                |
| `targetSlideIndex` | integer |          | jump to this page (0-based)                   |

## `set_element_stroke`

Set an element's stroke. Pass color (#RRGGBB) + widthPt (points); to remove the stroke pass remove=true.

| argument     | type    | required | description                    |
| ------------ | ------- | -------- | ------------------------------ |
| `slideIndex` | integer | yes      |                                |
| `sourceId`   | string  | yes      |                                |
| `color`      | string  |          | #RRGGBB                        |
| `widthPt`    | number  |          | Line width (points), default 1 |
| `remove`     | boolean |          | true = remove stroke           |

## `set_element_style`

Change an element's text formatting without changing the text: font size/color/bold/italic/underline/alignment/font. Pass only the fields to change; others stay as-is. Applies to the element's entire text.

| argument     | type                          | required | description                                  |
| ------------ | ----------------------------- | -------- | -------------------------------------------- |
| `slideIndex` | integer                       | yes      | Page number (0-based)                        |
| `sourceId`   | string                        | yes      | Element id                                   |
| `fontSize`   | number                        |          | Font size (pt)                               |
| `color`      | string                        |          | #RRGGBB                                      |
| `bold`       | boolean                       |          |                                              |
| `italic`     | boolean                       |          |                                              |
| `underline`  | boolean                       |          |                                              |
| `fontFamily` | string                        |          | Font name; usually omit to inherit the theme |
| `align`      | `left` \| `center` \| `right` |          |                                              |

## `set_element_text`

Replace a text element's entire content. paragraphs is the complete post-replacement paragraph array, one object per paragraph; whole-paragraph bold/italic etc. use the boolean fields on the paragraph object.

| argument     | type    | required | description                              |
| ------------ | ------- | -------- | ---------------------------------------- |
| `slideIndex` | integer | yes      | Page number (0-based)                    |
| `sourceId`   | string  | yes      | Element id (from the outline/read_slide) |
| `paragraphs` | any     | yes      |                                          |

## `set_element_transform`

Move/resize/rotate an element (pixel coordinates, origin top-left, canvas 1280 wide). Pass only the fields to change; others keep their values.

| argument      | type    | required | description                         |
| ------------- | ------- | -------- | ----------------------------------- |
| `slideIndex`  | integer | yes      |                                     |
| `sourceId`    | string  | yes      |                                     |
| `x`           | number  |          | Top-left x (px)                     |
| `y`           | number  |          | Top-left y (px)                     |
| `w`           | number  |          | Width (px)                          |
| `h`           | number  |          | Height (px)                         |
| `rotationDeg` | number  |          | Rotation angle (degrees, clockwise) |

## `set_header_footer`

Set the deck-wide footer, slide numbers and date shown on every page. footer is the footer text (empty string or null removes it); slideNum turns page numbers on or off; date is a fixed date string, or set dateAuto for a field that updates when the deck is opened. Omit a field to leave it as it is.

| argument   | type    | required | description                                                           |
| ---------- | ------- | -------- | --------------------------------------------------------------------- |
| `footer`   | string  |          | Footer text; empty string removes it                                  |
| `slideNum` | boolean |          | Show page numbers                                                     |
| `date`     | string  |          | Fixed date text; empty string removes it                              |
| `dateAuto` | boolean |          | Write the date as a field that updates on open, instead of fixed text |

## `set_slide_animations`

Replace a page's animation list (list order = play order). Pass an empty items array to clear. Use it to reveal a list a point at a time or to bring in a diagram in stages; animating everything is worse than animating nothing.

| argument             | type                                                                                                                                                                                                                          | required | description                                                |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------------------------------------------------------- |
| `slideIndex`         | integer                                                                                                                                                                                                                       | yes      |                                                            |
| `items`              | object[]                                                                                                                                                                                                                      | yes      | Play order; an element may appear more than once           |
| `items[].sourceId`   | string                                                                                                                                                                                                                        | yes      |                                                            |
| `items[].effect`     | `appear` \| `fade` \| `flyIn` \| `wipe` \| `wipeDown` \| `splitIn` \| `bounce` \| `flipIn` \| `zoom` \| `pulse` \| `spin` \| `grow` \| `teeter` \| `disappear` \| `fadeOut` \| `flyOut` \| `wipeOut` \| `shrink` \| `zoomOut` | yes      |                                                            |
| `items[].trigger`    | `onClick` \| `withPrev` \| `afterPrev`                                                                                                                                                                                        |          |                                                            |
| `items[].durationMs` | integer                                                                                                                                                                                                                       |          | default 500                                                |
| `items[].delayMs`    | integer                                                                                                                                                                                                                       |          | default 0                                                  |
| `items[].paragraph`  | integer                                                                                                                                                                                                                       |          | animate one paragraph (0-based) instead of the whole shape |

## `set_slide_background`

Set a solid page background color. slideIndex=-1 applies to all pages.

| argument     | type    | required | description                           |
| ------------ | ------- | -------- | ------------------------------------- |
| `slideIndex` | integer | yes      | Page number (0-based); -1 = all pages |
| `color`      | string  | yes      | #RRGGBB                               |

## `set_slide_notes`

Write a page's speaker notes (replaces them). Notes are the script the presenter reads — the argument the slide supports, the transition to the next page, the answer to the obvious question. Do not restate the slide text: the audience can already read it.

| argument     | type    | required | description                                                            |
| ------------ | ------- | -------- | ---------------------------------------------------------------------- |
| `slideIndex` | integer | yes      | Page number (0-based)                                                  |
| `text`       | string  | yes      | Notes text; blank lines separate paragraphs. Empty string clears them. |

## `set_slide_transition`

Set the page transition. slideIndex=-1 applies to the whole deck, which is usually what you want — mixed transitions look unplanned. 'morph' animates shapes shared between consecutive pages and needs those pages to be built for it; 'fade' is the safe default.

| argument     | type                                                                                                                            | required | description                           |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------- |
| `slideIndex` | integer                                                                                                                         | yes      | Page number (0-based); -1 = all pages |
| `kind`       | `none` \| `morph` \| `fade` \| `push` \| `wipe` \| `split` \| `circle` \| `cover` \| `pull` \| `dissolve` \| `zoom` \| `random` | yes      |                                       |

## `set_text_anchor`

Set where text sits vertically inside its box: top, middle or bottom. The usual fix when a short line floats at the top of a tall shape, or when captions across a row do not line up. Horizontal alignment is the paragraph's align, set with set_element_text.

| argument     | type                          | required | description           |
| ------------ | ----------------------------- | -------- | --------------------- |
| `slideIndex` | integer                       | yes      | Page number (0-based) |
| `sourceId`   | string                        | yes      |                       |
| `anchor`     | `top` \| `middle` \| `bottom` | yes      |                       |

## `ungroup_element`

Ungroup a group element: promote its direct children to top-level page elements (positions/sizes preserved). Use when group members must be edited/deleted independently (e.g. elements nested in a sub-group, or deleting a single member). Note: ungrouping rewrites the page, so all element ids on it change — use the fresh ids returned in the result.

| argument     | type    | required | description           |
| ------------ | ------- | -------- | --------------------- |
| `slideIndex` | integer | yes      | Page number (0-based) |
| `sourceId`   | string  | yes      | Group element id      |

## `web_search`

Web search for text information (material/data/facts). Use when you need current information or are unsure about facts. Returns title/link/snippet.

| argument     | type    | required | description            |
| ------------ | ------- | -------- | ---------------------- |
| `query`      | string  | yes      | Search keywords        |
| `maxResults` | integer |          | Max results, default 6 |
